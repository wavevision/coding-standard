os: linux
dist: xenial
language: php

git:
  depth: 1


before_script:
  - composer install

jobs:
  include:
    - script: composer lint
    - script: composer phpcs
    - script: composer phpstan
    - script: composer test

cache:
  directories:
    - $HOME/.composer/cache